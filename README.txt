Đặt các file sau vào thư mục này:

📷 ẢNH:
  img1.jpg đến img10.jpg (hoặc ít hơn)
  → Trong script.js > CONFIG.images, chỉnh lại đường dẫn cho khớp

🎵 NHẠC:
  music.mp3
  → Có thể dùng bất kỳ file nhạc .mp3 nào bạn thích

💡 LƯU Ý:
  - Mở bằng Live Server (VS Code) hoặc bất kỳ local server nào
  - KHÔNG mở trực tiếp bằng file:// (sẽ bị lỗi CORS với Three.js)
  - Nếu ảnh chưa có, website vẫn hiển thị ô màu đẹp thay thế
